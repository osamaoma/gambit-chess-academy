{
  "name": "@gambit/explanation-engine",
  "version": "0.1.0",
  "description": "Modular, principle-based chess move explanation engine — architecture core (no concrete detectors yet)",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:browser": "esbuild src/browser.ts --bundle --format=iife --global-name=GambitExplain --minify --outfile=../public/explanation-engine.js",
    "test": "tsc && node --test \"dist/tests/*.test.js\"",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.5.0"
  }
}
